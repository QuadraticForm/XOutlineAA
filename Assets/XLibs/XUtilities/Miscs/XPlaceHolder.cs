using UnityEngine;

public class XPlaceHolder : MonoBehaviour
{
    public string comment;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
